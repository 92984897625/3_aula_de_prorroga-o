programa {
  funcao inicio() {
    inteiro numero,soma=0
    faca {
     escreva ("Informe o numero pra somar: ")
       leia (numero)
      soma = soma + numero
    } enquanto(numero != 0)

    escreva ("Resultado da soma: ", soma)
  }
}
